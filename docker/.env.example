# Copy thành docker/.env rồi điền giá trị.
#
# JWT_SECRET: chuỗi ngẫu nhiên dài, sinh bằng:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=

# Port expose web ra máy host (default 8080)
WEB_PORT=8080
